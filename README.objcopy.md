# llvm-objcopy package

This package provides the llvm-objcopy binary as a standalone native tool for all targets.

Included utilities:
- llvm-objcopy: object file copier/stripper for section and symbol edits.
